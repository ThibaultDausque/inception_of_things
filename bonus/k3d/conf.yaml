apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: conf-file
servers: 1
ports:
  - port: 8888:80
    nodeFilters:
      - loadbalancer
files: 
  - source: ./namespace.yaml
    destination: /var/lib/rancher/k3s/server/manifests/custom/foo.yaml
options:
  k3d:
    wait: true
